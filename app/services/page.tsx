import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: "Services | Pixel 'N' Purpose",
  description: 'Refined digital services — website design, brand identity, packaging, and photography. We craft experiences that merge purpose with pixel-perfect precision.',
};

export { default } from './ServicesClient';
